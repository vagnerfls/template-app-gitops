{{- if .Values.frontend.imageStream -}}

apiVersion: image.openshift.io/v1
kind: ImageStream

metadata: 
  name: {{ .Values.applicationName }}-frontend

spec:
  lookupPolicy:
    local: true

{{- end -}}