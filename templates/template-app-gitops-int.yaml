apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: template-app-gitops-int
  namespace: openshift-gitops
spec:
  project: app
  syncPolicy:
    automated: {}
  destination:
    server: {{ .Values.spec.destination.server }}
    namespace: app-int
  source:
    path: int
    repoURL: {{ .Values.spec.source.repoURL }}