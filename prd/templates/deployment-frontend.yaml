{{- if .Values.frontend.enabled -}}

kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{ .Values.applicationName }}-frontend

  annotations:
    image.openshift.io/triggers: |
      [
        {"from":
          {"kind":"ImageStreamTag"
            , "name":"{{ .Values.applicationName }}-frontend:stable"
          }
          , "fieldPath":"spec.template.spec.containers[?(@.name==\"{{ .Values.applicationName }}-frontend\")].image"
        }
      ]


spec:
  replicas: {{ .Values.frontend.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.applicationName }}-frontend
  template:
    metadata:
      labels:
        app: {{ .Values.applicationName }}-frontend
    spec:
      containers:
        - name: {{ .Values.applicationName }}-frontend
          image: {{ .Values.applicationName }}-frontend:stable
          ports:
            - containerPort: {{ .Values.frontend.containerPort }}
              protocol: TCP
              name: http
          resources:
            limits:
              cpu: {{ .Values.frontend.cpuLimit }}
              memory: {{ .Values.frontend.memoryLimit }}
            requests:
              cpu: {{ .Values.frontend.cpuRequest }}
              memory: {{ .Values.frontend.memoryRequest }}

            livenessProbe:
              httpGet:
                path: /{{ .Values.applicationName }}/
                port: 8080
              initialDelaySeconds: 3
              periodSeconds: 3
            readinessProbe:
              httpGet:
                path: /{{ .Values.applicationName }}/
                port: 8080
              initialDelaySeconds: 3
              periodSeconds: 3

{{- end -}}