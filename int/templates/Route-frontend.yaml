{{- if .Values.frontend.route -}}

kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: {{ .Release.Name | trimSuffix "-int" }}-frontend


spec:
  host: int-{{ .Release.Name | trimSuffix "-int" }}-frontend.apps.ocp.sefaz.pe.gov.br
  to:
    kind: Service
    name: {{ .Release.Name | trimSuffix "-int" }}-frontend
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect

{{- end -}}