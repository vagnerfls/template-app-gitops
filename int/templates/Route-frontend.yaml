{{- if .Values.frontend.route -}}

kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: {{ .Values.applicationName }}-frontend


spec:
  host: int-{{ .Values.applicationName }}-frontend.apps.ocp.sefaz.pe.gov.br
  to:
    kind: Service
    name: {{ .Values.applicationName }}-frontend
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect

{{- end -}}