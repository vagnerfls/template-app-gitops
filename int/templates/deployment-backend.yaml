{{- if .Values.backend.enabled -}}

kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{ .Release.Name | delimeter 3 }}-backend

  annotations:
    image.openshift.io/triggers: |
      [
        {"from":
          {"kind":"ImageStreamTag"
            , "name":"{{ .Release.Name | delimeter 3 }}-backend:latest"
          }
          , "fieldPath":"spec.template.spec.containers[?(@.name==\"{{ .Release.Name | delimeter 3 }}-backend\")].image"
        }
      ]


spec:
  replicas: {{ .Values.backend.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name | delimeter 3 }}-backend
  template:
    metadata:
      labels:
        app: {{ .Release.Name | delimeter 3 }}-backend
    spec:
      containers:
        - name: {{ .Release.Name | delimeter 3 }}-backend
          image: {{ .Release.Name | delimeter 3 }}-backend:latest
          ports:
            - containerPort: {{ .Values.backend.containerPort }}
              protocol: TCP
              name: http
          resources:
            limits:
              cpu: {{ .Values.backend.cpuLimit }}
              memory: {{ .Values.backend.memoryLimit }}
            requests:
              cpu: {{ .Values.backend.cpuRequest }}
              memory: {{ .Values.backend.memoryRequest }}
          
          env: 
            {{- toYaml .Values.backend.env | nindent 12 }}

          {{ if .Values.secrets }}
          {{ range $secret := .Values.secrets }}
          envFrom:
            secretGenerator:
              - name: {{ $secret.name }}
          {{ end }}
          {{ end }}


          livenessProbe:
            {{- toYaml .Values.backend.livenessProbe | nindent 12}}
          readinessProbe:
            {{- toYaml .Values.backend.readinessProbe | nindent 12}}


{{- end -}}